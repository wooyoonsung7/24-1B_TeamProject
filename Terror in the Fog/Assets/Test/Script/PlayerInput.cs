using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UIElements.Experimental;

public class PlayerInput : MonoBehaviour
{/*
    public string moveAxisName_01 = "Vertical";
    public string moveAxisName_02 = "Horizontal";
    //public string select_Item = "Mouse ScrolWheel";
    public string runButton = "Run";
    public string crouchButton = "Crouch";
    public string interaction = "Interaction";
    public string useItem = "UseItem";


    public float moveVertical { get; private set; }
    public float moveHorizontal { get; private set; }
    //public float selectItem { get; private set; }
    public bool run {  get; private set; }
    public bool crouch { get; private set; }
    public bool interact { get; private set; }
    public bool use { get; private set; }


    void Update()
    {
        //게임오버 상태에서는 사용자 입격을 감지하지 않음

        moveVertical = Input.GetAxis(moveAxisName_01);
        moveHorizontal = Input.GetAxis(moveAxisName_02);
        //selectItem = Input.GetAxis(select_Item);
        run = Input.GetButton(runButton);
        crouch = Input.GetButtonDown(crouchButton);
        interact = Input.GetButton(interaction);
        use = Input.GetButtonDown(useItem);

        //Debug.Log(run);
    }*/
}
